<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<configuration>
  <!--
    S3A settings for the Hive Metastore talking to MinIO.
    Credentials come from the AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY
    environment variables (set on the hive-metastore service), so no secrets
    live in this file.
  -->
  <property>
    <name>fs.s3a.endpoint</name>
    <value>http://minio:9000</value>
  </property>
  <property>
    <name>fs.s3a.path.style.access</name>
    <value>true</value>
  </property>
  <property>
    <name>fs.s3a.connection.ssl.enabled</name>
    <value>false</value>
  </property>
  <property>
    <name>fs.s3a.aws.credentials.provider</name>
    <value>com.amazonaws.auth.EnvironmentVariableCredentialsProvider</value>
  </property>

  <!--
    Trino writes table/schema locations with the s3:// (and s3n://) scheme;
    map those onto the S3A filesystem so the metastore can create the dirs.
    S3A still reads its fs.s3a.* settings above regardless of the URI scheme.
  -->
  <property>
    <name>fs.s3.impl</name>
    <value>org.apache.hadoop.fs.s3a.S3AFileSystem</value>
  </property>
  <property>
    <name>fs.s3n.impl</name>
    <value>org.apache.hadoop.fs.s3a.S3AFileSystem</value>
  </property>
  <property>
    <name>fs.AbstractFileSystem.s3.impl</name>
    <value>org.apache.hadoop.fs.s3a.S3A</value>
  </property>
</configuration>
